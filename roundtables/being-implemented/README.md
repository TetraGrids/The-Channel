# Being implemented

Finalized meetings: ordered Cursor prompts, one file each (`YYYY-MM-DD-short-kebab-topic.md`).

Run prompts in the order listed in the file. When the work is done, **move** the file to `../completed/`.
